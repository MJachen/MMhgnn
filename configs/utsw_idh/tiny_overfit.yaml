base_config: "server_base.yaml"

output_dir: outputs/utsw_idh/tiny_overfit_seed42

data:
  split_json: splits/utsw_idh/utsw_idh_tiny_overfit_split.json
  allow_subset_split: true

train:
  epochs: 200
  batch_size: 1
  early_stop_patience: 200

calibration:
  enable_threshold_calibration: false

eval:
  test_all_combos: false
