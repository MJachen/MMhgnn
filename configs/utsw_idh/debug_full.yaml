base_config: "server_base.yaml"

# Supply the data root at runtime with --data-root.
output_dir: outputs/utsw_idh/debug_full_seed42

data:
  split_json: splits/utsw_idh/utsw_idh_debug_split.json
  allow_subset_split: false

train:
  epochs: 20
  early_stop_patience: 8

calibration:
  enable_threshold_calibration: false

eval:
  test_all_combos: false
